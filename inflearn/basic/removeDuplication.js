function solution(str) {
  let answer = [];
  let strArray = str.split('');
  for (let x of str) {
    if (strArray.indexOf(x) === -1) console.log(x);
  }
  return answer;
}

let = 'ksekkset';

console.log(solution(str));
